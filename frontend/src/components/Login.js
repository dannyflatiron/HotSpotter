import React from 'react'

const Login = () => {
    return (
        <form onSubmit={}>
            <input type="text" onChange={} value={} name={username}/>
            <input type="text" onChange={} value={} name={password}/>
            <input type="submit" value="Log In"/>
        </form>
    )
}

export default Login